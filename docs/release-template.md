# Release template

Suggested release title:

Relay Console vX.Y.Z — single-file offline AI relay tool

Opening sentence:

Relay Console is a local, offline, single-file HTML tool for manually relaying conversations between multiple AI assistants.

## What changed

- 
- 
- 

## Download

Download `relay-console-vX.Y.Z.html`, save it locally, and open it in a modern browser.

No API keys, backend, build step, account, or installation required.

## Suggested release assets

- `relay-console-vX.Y.Z.html` for versioned provenance.
- `relay-console.html` as an optional stable filename for nontechnical users.

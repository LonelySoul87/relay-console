# Relay Console v2.4.0 progress

## Baseline verified on 28 August 2026

- `main`, `origin/main`, and tag `v2.3.0` point to the published merge commit
  `f93923b`.
- The main working tree was clean and only `main` remained locally and remotely.
- GitHub reports no open issue.
- The available repository traffic window reports 7 views from 2 unique
  visitors.
- CI passes 130 checks. The local main clone passes 134 because four ignored
  legacy session fixtures add one conditional check to each suite.
- The v2.3.0 release asset, checksum attachment, repository artifact, live Pages
  file, and permanent Deliverables copy are byte-identical.

## Development scaffold

- Created the isolated `develop/v2.4.0` worktree from the published v2.3.0
  merge commit.
- Created `relay-console-v2.4.0-draft.html` without modifying the v2.3.0
  release artifact.
- Added a dedicated v2.4 suite while leaving the live index and checksum
  integrity assertions under v2.3 until the next release migration gate.
- Changed CI to use Node's automatic test discovery so future versioned suites
  cannot be added without being executed.
- Added a broad polish roadmap for safer imports, preset management, transcript
  navigation, recovery, accessibility, exports, and possible language growth.

## Scaffold verification

- All five suites pass independently with 177 checks total and no failures.
- The exact `node --test` automatic-discovery command also finds and passes all
  177 checks.
- Desktop and 390 by 844 pixel browser smoke checks confirm the v2.4 draft title
  and badge with no horizontal overflow or console warning or error.
- The v2.3.0 artifact, `index.html`, checksum manifest, tag, and public release
  remain unchanged.

## Independent scope audit

- Claude Code independently verified the scaffold and automatic test discovery.
- The audit selected **Nothing is lost by accident** as the v2.4 release theme.
- Four inherited defects were reproduced: invalid ballot recovery, disagreement
  between ballots and exports, unconfirmed resume discard, and focus loss after
  changing the interface language during a relay.
- Language expansion moved to v2.5, right-to-left support remains a separate
  architectural decision, and generated diagnostics were rejected.

## First implementation checkpoint

- Fixed invalid ballot recovery. A rebuilt dropdown now starts from the visible
  label set, and a manually corrected ballot always offers direct re-parsing.
- Added one validated ballot helper shared by the tally, transcript badge,
  transcript export, and review packet so they cannot disagree about whether a
  ranking counts.
- Added confirmation before the saved-session Discard action can erase its local
  autosave.
- Preserved focus on the interface-language selector during an active relay.
- Added a modal import preview for sessions and preset bundles. Files are fully
  validated before preview, and nothing changes until the user explicitly
  applies the import.
- Session previews show version, participants, turns, captured answers, current
  position, plan, and both languages.
- Preset previews show each normalized preset and warn about collision renames,
  removed unsafe URLs, and shortened roles.
- Imported strings are rendered with `textContent`; the active artifact still
  has exactly one non-empty `innerHTML` assignment, limited to trusted catalog
  copy.

## Verification at this checkpoint

- The v2.4 suite now passes 53 checks, up from 47 at the scaffold.
- Automatic discovery passes all 183 checks across the five historical and
  current suites with no failures or skips.
- A live browser check confirmed the import dialog structure, explicit apply and
  cancel controls, keyboard focus retention after a language change, and a valid
  three-participant ballot parse.
- Desktop and 390 by 844 pixel screenshots show the import preview without
  horizontal overflow. The phone-width body reports equal client and scroll
  widths, and the browser console has no warnings or errors.
- Active project and product text still contains no Unicode em dash character.
- The v2.3.0 artifact, live index, checksum manifest, and all prior release files
  remain unchanged.

## Independent implementation audit

Claude Code independently audited commit `e7debe7` and passed all 15 requested
verification points. The audit also found a preset-import race and four smaller
polish gaps.

## Audit correction checkpoint

- Preset imports now recompute against current storage when Apply is pressed.
  If another tab changed the presets while the review was open, Relay Console
  performs no write, refreshes the complete review, preserves the concurrent
  change, and asks the user to review again.
- Website addresses over 300 characters are now removed rather than silently
  shortened into a different usable address, and the preview reports the
  removal.
- The import dialog now describes its summary to assistive technology and puts
  initial focus on the review heading instead of the Apply action.
- The specific over-26-participant explanation is restored for saved-preset
  loading and portable-preset importing.
- Removed the obsolete pre-dialog import confirmation from all three language
  catalogs while preserving exact catalog and placeholder parity.
- The v2.4 suite passes 55 checks and automatic discovery passes all 185 checks.
  New coverage proves the concurrent preset is
  preserved with zero writes on the refresh, session descriptions do not touch
  storage, safe dialog focus is used, overlong URLs are reported, and the
  participant-limit message is specific.
- Desktop and 390 by 844 pixel browser checks used a maximum-size 50-preset
  bundle with 100 normalization warnings. The dialog stayed within the viewport
  without horizontal overflow, scrolled to both actions, focused its heading,
  returned focus to Import after Cancel, and produced no console warnings or
  errors.

## Recovery and autosave batch, implemented by Claude Code

Claude Code implemented this batch directly on `develop/v2.4.0` under equal
co-developer authority, starting from `a04e7c8`.

### Delivered

- One bounded recovery slot at `relayConsole.recovery.v1`. Never a history: a
  new capture replaces the previous one.
- Capture immediately before restart, saved-relay discard, and an imported
  session replacing existing work. Empty relays are skipped entirely.
- Seven-day expiry, evaluated on read. Expired or unusable records are removed
  the moment they are seen rather than lingering until a timer fires.
- A one-megabyte serialized ceiling. An oversize relay is refused whole, nothing
  is truncated, and the alert points the user to Save file.
- Restoration runs through `validateSession`, so a hand-edited recovery record
  is treated exactly as cautiously as an imported file.
- Visible Restore and Remove controls in a bar that sits outside both the setup
  and run panels, so it survives the switch between them. Remove is confirmed.
- An autosave status line with the last successful local save time. The
  timestamp never advances on a failed save, and a failure stays visibly failed
  until a save succeeds.
- A storage report in the presets panel covering the total plus the session,
  presets, and recovery slots, with quota guidance that names the specific
  controls to use when space runs short.
- Complete English, French, and Spanish copy, including a privacy note stating
  that discarded or replaced work can remain in local storage for up to seven
  days unless removed.

### Verification

- The v2.4 suite passes 67 checks. Automatic discovery passes 197 checks across
  all five suites, with no failures and no skips.
- Every expiry assertion passes an explicit timestamp. No test in this batch
  reads the real clock, so the suite cannot drift with wall time.
- New coverage: successful capture and restore, single-slot replacement, the
  exact seven-day boundary on both sides, six shapes of unusable recovery data,
  oversize refusal with nothing stored, empty-relay skipping, immediate Remove
  and a declined Remove, all three destructive capture points, save-failure
  visibility, storage accounting including the recovery slot, quota guidance,
  and three-language copy including the French recovery bar.
- Catalog parity re-verified independently: 370 keys in each of English, French,
  and Spanish, identical key sets, zero placeholder mismatches, zero em dashes
  in the artifact.
- The released v2.3.0 artifact, `index.html`, `SHA256SUMS.txt`, `README.md`,
  `landing.html`, and every earlier artifact and tag are untouched.

### Browser verification not performed in this batch

Claude Code could not run an interactive browser check. The available browser
tool renders a file outside its own project folder as a static snapshot on a
`data:` origin, where scripts do not execute and `localStorage` is unavailable,
so the result would not have exercised any of this code. No bypass was
attempted and no browser evidence is claimed.

As partial compensation the new surfaces reuse the already-verified `.resume`
bar layout, and a structural test locks the wrapping and announcement rules:
`flex-wrap:wrap` on the recovery bar and save status, `overflow-wrap:anywhere`
on all three new surfaces, `role="status"` with `aria-live="polite"` on the
save status, and the recovery bar's position outside both panels. The
`overflow-wrap` rules were added specifically because the recovery message
embeds up to sixty characters of the user's question, which can be a single
unbroken token.

Claude Code did not claim an interactive browser pass for this batch. ChatGPT
completed that pass during the peer review described below.

## Peer hardening and browser checkpoint

ChatGPT reviewed Claude Code's recovery batch as equal-authority implementation
work and then corrected the interaction edges found during adversarial review.

### Corrections

- Starting a new relay now protects an offered saved relay in recovery before
  replacing the session slot.
- Failed and oversize recovery writes pause restart, discard, and imported-file
  replacement. Cancel preserves the current work; continuing requires an
  explicit confirmation.
- A restored checkpoint remains available until the restored relay autosaves
  successfully. A storage failure cannot silently consume the only recovery
  copy.
- Future-dated recovery records are rejected, and empty relays skip recovery
  before browser-storage availability is considered.
- Storage reporting now estimates the UTF-16 browser strings actually stored,
  labels the result as an estimate, avoids a fixed quota claim, and refreshes
  after setup and preset changes.
- Restart and discard copy now explains the seven-day recovery window instead
  of saying the work cannot be recovered.
- Restore and Remove return keyboard focus to the next relevant setup control.
- A live phone-width test found one additional defect: an unbroken question in
  the saved-relay banner widened the page. The banner now has a flexible,
  wrapping message region matching the recovery bar.

### Verification

- The v2.4 suite passes 70 checks, including direct coverage for offered-session
  preservation, failed recovery writes, delayed checkpoint consumption,
  future timestamps, removal focus, exact storage estimation, live report
  refresh, and narrow banner wrapping.
- Automatic discovery passes 200 checks across all five suites with no failures
  or skips.
- A live browser pass used the real app through a local HTTP origin. At 390 by
  844 pixels, simultaneous Spanish resume and recovery banners fit a 375-pixel
  document width with no horizontal overflow. The test included a 60-character
  unbroken question.
- The longest Spanish storage report wrapped within its container. Cancelling
  recovery removal kept both saved surfaces visible. The browser console
  reported no warnings or errors.
- Storage-failure and successful removal paths remain covered directly by the
  automated browser harness, where their state and focus results can be asserted
  without deleting the live browser test data.

## Claude Code audit of the peer-hardening batch

Claude Code audited every change from `620bc2f` through `71d21c1` by executing
the artifact, not by reading the patch. Ten of ChatGPT's eleven corrections held
up exactly as described. Four defects were found and fixed directly.

### Defects found and corrected

1. **A pending restore consumption could delete an unrelated checkpoint.**
   `consumeRecoveryOnSave` was a bare boolean. A restore whose autosave failed
   armed it, and it then survived both Restart and Remove, so the next ordinary
   successful autosave deleted whatever checkpoint existed by that point.
   Reproduced twice: once where Restart had just captured a fresh copy of the
   very relay being protected, and once where a later unrelated capture was
   destroyed. Both paths lost user work permanently. The flag is now
   `consumeRecoveryAt`, holding the `savedAt` of the record the restore came
   from, and a save consumes the slot only when the stored record still matches.
   A new capture and a manual Remove both disarm it.

2. **A backward clock correction destroyed a valid checkpoint.** The
   future-date rejection had zero tolerance, so a record one millisecond ahead
   of the clock was refused, and `refreshRecoveryOffer` then deleted it. An
   ordinary NTP correction, a virtual machine resume, or a dual-boot clock
   mismatch was enough to lose a good checkpoint. A one-day skew allowance is
   now tolerated, well inside the seven-day window, while a genuinely bogus date
   is still rejected.

3. **Two different units in the same panel.** `recoverySize` measured UTF-8
   bytes through `Blob` while `Store.usage` measured UTF-16, so a checkpoint
   that had just passed a one-megabyte ceiling was displayed as 2.00 MB in the
   storage report beside it. Both now use one `storageBytes` helper measuring a
   conservative two bytes per JavaScript code unit. Actual quota accounting is
   implementation-defined, so the figure remains explicitly labeled as an
   estimate. The tradeoff is that the effective ceiling halves in character
   terms, which is the safe direction and keeps the two app surfaces consistent.

4. **The storage report was recomputed on every keystroke.** It had been wired
   into `saveSetupDraft`, `savePref` and `persistRoster`, all of which fire per
   character. Measured at seven `localStorage` reads and roughly 400,000
   characters re-read for a single keystroke with a large prior session stored.
   All three now route through a coalescing `scheduleStorageReport`, so a burst
   of typing costs one measurement instead of one per character.

### Confirmed working as described

- Restore keeps its checkpoint while autosave fails and consumes it on the first
  save that succeeds.
- The one-megabyte boundary is exact: the largest fitting relay saves and one
  character more is refused with nothing stored.
- Focus after Remove lands on the answer field during a relay, the Resume button
  when a saved session is offered, and the question field otherwise. Focus after
  Restore lands on the answer field.
- Confirmation ordering during restart and discard, the failed-write choice, the
  oversize cancel path, empty-work skipping, and expiry all behave as claimed.

### Verification

- The v2.4 suite passes 73 checks. Automatic discovery passes 203 across all
  five suites, with no failures and no skips.
- Three new behavioral tests cover the scoped restore consumption across
  Restart and Remove, single-unit size reporting, and coalesced storage
  measurement. The future-date test was widened to assert both edges of the new
  skew tolerance.
- Catalog parity re-verified: 371 keys in each language, identical key sets,
  zero placeholder mismatches, zero em dashes in the artifact.
- The privacy boundary is intact and the artifact still contains exactly one
  non-empty `innerHTML` assignment, limited to trusted catalog copy.
- Every released v2.3 surface, artifact, and tag is unchanged.

### Not verified by Claude Code

No interactive browser pass. The available tool renders a file outside its own
project folder as a static snapshot on a `data:` origin where scripts do not
execute, so nothing would have been exercised. ChatGPT's live 390 by 844 results
are taken as reported. Claude could not demonstrate the coalescing behavior in
that environment, so it still required a real browser pass.

## ChatGPT audit of Claude Code's corrections

ChatGPT reproduced Claude Code's four defects and confirmed that all four fixes
improved the product. The untouched branch passed all 203 checks. One surviving
variant of the checkpoint-consumption data loss was then reproduced and fixed.

### Additional correction

- If a restored relay could not replace its checkpoint during Restart, and the
  user explicitly continued without the replacement, the earlier consumption
  marker remained armed. A later successful save could still delete the older
  checkpoint. The marker is now disarmed whenever a destructive action is
  allowed to proceed, including the failed and oversize paths.
- Checkpoint consumption now compares the exact serialized record rather than
  only `savedAt`. Another tab or a manual storage edit cannot lose a different
  record merely because it was written in the same millisecond.
- A new behavioral regression reproduces both cases. It failed on `8844c13`
  before the implementation change and passes afterward.

### Browser and measurement verification

- A real browser confirmed the 500 ms coalescing window. Rapid question edits
  left the storage report unchanged immediately and after 200 ms, then produced
  the final value after the window elapsed.
- The same test passed with a 422.8 KB saved session, covering the expensive case
  that motivated the optimization. The browser console reported no warnings or
  errors.
- A 930.6 KB saved session became a 930.8 KB recovery checkpoint. The source and
  recovery figures now agree within the recovery key overhead instead of
  differing by a factor of two.
- The test harness now queues timers explicitly, so three rapid writes schedule
  one 500 ms measurement and the callback can be executed and counted. The
  earlier structural-only assertion is no longer the sole automated evidence.
- Standards wording was tightened: Web Storage values are strings, while usage
  and quota accounting are implementation-defined estimates. Relay Console uses
  a consistent conservative code-unit estimate and does not claim it is the
  browser's exact physical accounting.
- The v2.4 suite passes 74 checks. Automatic discovery passes 204 checks across
  all five suites, with no failures or skips.

## Claude Code reciprocal audit of the recovery edge cases

Claude Code audited `8844c13..ce48dc4` by executing the artifact. Every claim in
that range reproduced.

### Confirmed

- The surviving D1 variant is genuinely closed on both continuation paths. With
  a restore whose autosave failed and a Restart whose replacement write then
  fails, the older checkpoint survives the continuation and survives a later
  successful save. The same holds when the replacement is refused for being
  oversize.
- Declining the continuation correctly keeps the pending consumption armed, so
  the restored workflow still consumes its own checkpoint on the next good save.
- Exact-record identity is a real improvement over `savedAt`. A record written
  in the same millisecond by another tab is preserved, which the timestamp
  comparison could not guarantee. A byte-identical rewrite is still consumed,
  which loses no distinct work.
- A stale in-memory offer restores older content but never deletes the newer
  record in storage, so no work is lost by that race either.
- The deterministic timer queue is accurate. Three rapid writes schedule exactly
  one 500 ms measurement and the callback measures exactly once.

### Corrected

The timer queue upgrade had one side effect worth closing. Deferring
`setTimeout` means the app's five other deferred callbacks no longer execute
during the suite at all, where the previous synchronous stub at least ran them.
A throwing callback could pass unnoticed. A new test now drains every pending
callback and asserts none throws, and it also asserts the one visible outcome
the suite had never checked: the preset status auto-clear and its sequence
guard, including that an older timer cannot clear a newer message.

ChatGPT's verification tightened that new test so it creates and asserts all
five callback families directly: prompt-copy status, mini-copy reset, object-URL
cleanup, import-dialog focus return, and preset-status clearing. It now checks
the exact timer delays plus every visible outcome, rather than assuming that
callbacks from uncalled paths were already pending. The suite remains at 205
checks because the existing test was strengthened rather than supplemented.

### Residual notes, neither worth a change

- `captureBeforeDestructive` disarms a pending consumption even when it returns
  `skipped`. Unreachable in practice, because the only calls made while a
  consumption is pending pass a session that has meaningful work. Either
  behaviour is lossless.
- `restoreRecovery` reads the cached in-memory offer rather than re-reading
  storage, unlike the preset import path which re-derives. Confirmed lossless:
  the newer record survives. Worth aligning only if recovery ever gains a second
  slot.

### Verification

- The v2.4 suite passes 75 checks. Automatic discovery passes 205 across all
  five suites, with no failures and no skips.
- Catalog parity: 371 keys in each language, identical key sets, zero
  placeholder mismatches, zero em dashes in the artifact.
- Privacy boundary intact, exactly one non-empty `innerHTML` assignment, seven
  storage keys, and every released v2.3 surface and tag unchanged.
- No interactive browser pass by Claude Code. The available tool renders a file
  outside its own project folder as a static snapshot where scripts do not run.
  The live coalescing and large-session measurements are taken as reported.

## Claude Code review of the deferred-callback completion

The test-coverage correction in `f10f9fe` is fair and the expanded test is
better than the one it replaces. Claude Code's original drain test triggered
three of the five families directly, not five, and the claim was overstated.
The replacement creates one callback from every family, pins the exact delay
set, drains all of them, and asserts each visible outcome. The `T0` comment
correction is also right.

No remaining product defect was found in that commit.

### One accessibility gap found and closed

The recovery banner is the only banner in the application that appears as a
direct result of a user action. The resume banner only ever appears at page
load, but the recovery banner arrives mid-flow after Restart, after discarding
a saved relay, and after an import replaces existing work. Demonstrated
behaviorally: the bar is hidden before Restart and visible immediately after,
with no announcement.

Its message now carries `role="status"` and `aria-live="polite"`, matching the
save status and preset status regions. The two buttons carry contextual
accessible names, following the convention the participant controls already
use, because "Restore" and "Remove" read as bare verbs out of context. The
buttons deliberately sit outside the live region so a re-render cannot
re-announce their labels, and the resume banner is deliberately left unchanged
because a live region there would speak at page load for no reason.

### Verification

- The v2.4 suite passes 76 checks. Automatic discovery passes 206, no failures,
  no skips.
- Catalog parity: 373 keys in each language, identical key sets, zero
  placeholder mismatches, zero em dashes in the artifact.
- The new test asserts the banner arrives from a real Restart, that the live
  region is an empty span with the correct role, that both buttons carry
  `data-i18n-aria`, and that the contextual names exist in all three languages
  and differ from the bare button text.
- Privacy boundary intact, one non-empty `innerHTML` assignment, seven storage
  keys, every released v2.3 surface and tag unchanged.

### ChatGPT reciprocal accessibility hardening

The live-region role and contextual button names were the right correction.
The first implementation still placed that region inside a parent carrying
`display:none`. Content hidden that way is absent from the accessibility tree,
so revealing a completed status region was not a reliable announcement across
screen readers. A dedicated visually hidden status region now remains rendered
from page load and receives the same recovery message when the visible banner
appears. It carries explicit `aria-atomic="true"` because current W3C guidance
notes that some environments do not apply the `status` role's implicit atomic
behavior.

The renderer suppresses an identical second announcement when Restart returns
to setup and renders the recovery surface again. The recovery test asserts the
announcement content, the always-rendered sibling markup, that one user action
writes the announcement only once, and that hiding the banner clears both the
visible message and its announcement channel.

## Claude Code review of the announcement channel

The interoperability correction in `7602b4d` is right. A live region inside a
`display:none` container is absent from the accessibility tree while its content
is written, and revealing a completed region afterwards is not a dependable
announcement pattern across screen readers. The always-rendered, clipped
sibling is the correct arrangement.

### Lifecycle audit

Traced every write to `recoveryOffer` and every touch of the two elements.
`renderRecoveryBar` is the single writer, and all four assignment sites call it
immediately, so the visible banner and the announcement channel cannot drift
apart. Exercised behaviorally:

- Restart renders twice and produces exactly one announcement naming the
  recovered question.
- Remove clears the banner, the visible message, and the channel together.
- A successful Restore clears all three.
- A Restore whose autosave fails re-shows the banner without writing to the
  channel at all, so nothing is repeated.
- An interface language change re-announces in the new language and still names
  the question.
- No transition was found that hides the banner while the channel still holds
  text. Expiry, external clearing by another tab, and the save-consumption path
  all clear both.

### One hollow regression test, corrected

The new duplicate-suppression test could not fail. Its instrumented setter
counted only assignments that changed the value, so an application that
assigned the same string twice would still have registered one write. In a
browser an identical reassignment is still a DOM mutation and a live region
announces it again, which is precisely what the guard in `renderRecoveryBar`
prevents.

Demonstrated by removing that guard: the suite still passed 76 of 76. The
counter now records every assignment, and the same experiment fails as it
should. The artifact was restored byte-identical after the experiment, verified
by checksum.

### Residual, not changed

At this checkpoint, suppression compared rendered text. Two different
checkpoints whose messages render identically, meaning the same reason,
question, answered count and displayed second, would suppress the second
announcement. Claude Code found no reachable sequence that produced it, and
the alternative of always re-announcing would reintroduce the double
announcement on Restart that this commit fixed.

### Verification

- The v2.4 suite passes 76 checks. Automatic discovery passes 206, no failures,
  no skips.
- Catalog parity: 373 keys per language, identical key sets, zero placeholder
  mismatches, zero em dashes in the artifact.
- A genuine screen reader pass is still the one outstanding manual check.

## ChatGPT follow-up on identical summaries

Claude Code's hollow-test correction is right: the counter now records every
assignment and proves that removing the application guard makes the test fail.

The remaining text-collision case is reachable. A recovery banner can remain
visible while a new relay starts, and a second destructive action can replace
the checkpoint with different captured content that has the same reason,
displayed timestamp, question, and answer count. The visible sentence is then
identical even though the stored work is not.

Announcement suppression now compares the exact serialized checkpoint identity
as well as the rendered sentence. A second render of one record remains silent,
while a distinct record is announced even if its summary happens to be the same.
The existing accessibility test now constructs two such records with different
answer bodies, proves their visible summaries match, and requires exactly one
new live-region assignment for the replacement. Test totals remain 76 for v2.4
and 206 overall because the existing test was strengthened again.

## Claude Code review of the checkpoint-identity suppression

The residual Claude Code recorded as unreachable was reachable, and the
identity-aware guard is the right fix. Reviewed by attempting to break it.

### Attempts that failed to break it

- One capture followed by twelve renders produces exactly one announcement.
- Four kinds of genuinely different work that render an identical sentence,
  differing only in the answer body, in trailing whitespace, in a participant
  name, and in the session marker, are each announced. Nothing was suppressed.
- Fifty consecutive refreshes of one unchanged checkpoint produce zero further
  announcements, so re-parsing storage does not manufacture a new token.
- Clearing the offer resets the token, so an identical checkpoint offered later
  speaks again, which is correct because the banner disappeared in between.
- Restart still announces once across its two renders, and a restore whose
  autosave fails still writes nothing.
- The announcement token and the consumption identity are produced by the same
  function on the same stored bytes.

No case was found where the token suppresses new work or repeats one record.

### One uncovered half of the guard, now covered

The suppression is a compound condition over the checkpoint identity and the
rendered sentence. Removing the identity half fails the accessibility test, as
intended. Removing the text half left all 206 checks passing.

The consequence of that gap is real. With identity-only suppression, changing
the interface language while a recovery banner is showing leaves the
announcement channel holding the previous language while the visible banner is
translated. Measured: zero assignments and English text retained after
switching to French.

A focused test now covers that half. It captures one checkpoint, switches the
interface language, and requires exactly one new assignment carrying the
translated sentence, the original question, and no stale English. Verified by
removing the text half again, which leaves this test failing and the rest of
the suite green.

### Verification

- The v2.4 suite passes 77 checks. Automatic discovery passes 207, no failures,
  no skips.
- Catalog parity: 373 keys per language, identical key sets, zero placeholder
  mismatches, zero em dashes in the artifact.
- The artifact was restored byte-identical after each experiment, verified by
  checksum. This change touches only the test file and this document.
- A real screen reader listening pass remains the one outstanding manual check.

## ChatGPT verification of the locale operand

The new focused test is genuine. Removing only the rendered-text operand from
the compound guard makes it fail with zero announcement assignments; restoring
the operand makes it pass. The test was also made independent of prior harness
state by setting English explicitly and using the shared fixed timestamp for
every recovery operation. This keeps its purpose narrow: one unchanged
checkpoint must be announced once when its rendered language changes.

## Claude Code audit of the preset library and export batch

Audited `657fc27` with a nine-dimension multi-agent sweep plus an independent
browser session. Everything was exercised by running code, and every fix below
was proved by deliberately reverting it on isolated copies and watching the suite
fail.

### The browser gate is no longer blocked

Both of us had recorded the local `file:` preview as refused. Serving the draft
from a localhost HTTP origin instead gives a real origin where scripts execute and
`localStorage` works, so desktop, phone-width, and reflow checks can now be done
directly. Results on the unmodified draft: no console output at all, no horizontal
overflow at 375 or at 320 CSS pixels, zero elements past the right edge, and the
preset disclosure fully readable with its buttons wrapping to two rows at phone
width and three at 320. The 320 pixel check is the WCAG 1.4.10 reflow equivalent
of 400 percent zoom and is more faithful than a `body.style.zoom` hack.

### Fixed: actions targeted a preset the control was not showing

`Number("")` is `0`, so an empty select value passed every "valid index" guard in
the preset code. On a fresh load with saved presets the dropdown showed nothing
selected, `selectedIndex` was `-1`, and yet the summary described preset 0 and all
five actions targeted it. Reproduced in the browser: with four presets stored and
nothing selected, pressing Delete removed the first one.

Six call sites read the selection by coercion. They now use one strict reader that
returns `-1` for anything that is not a run of digits, and `renderPresets` always
leaves a real option selected while the library is non-empty, so the control, the
summary, and the actions cannot disagree.

The existing 86 checks could not have caught this: the harness modelled
`select.value` as a free string with no option list, which makes an unselected
dropdown indistinguishable from a correctly selected one. The harness now
implements select semantics, and that alone turns the defect into a test failure.

### Fixed: failed writes reported the wrong operation and left a success line

A failed delete alerted "Could not save the preset" while the status line still
read "Preset deleted: Alpha". The alert now names deleting, every failing write
clears the stale success line first, and the delete confirmation falls back to the
same positional label the dropdown uses instead of announcing "undefined" for an
unnamed entry.

The suite had pinned the misleading wording, so that assertion was retargeted
rather than worked around.

### Fixed: preset identity was computed two different ways

Collision checks compared a fully normalized new name against a merely trimmed
stored one. Releases up to v2.2.0 wrote names without stripping angle brackets, so
an upgraded library holding `Q<A` would accept a second preset named `QA`: two
entries identical under the app's own identity function, with identical inspection
titles. All four comparison sites now use one `presetNameKey` helper. This is
pre-existing rather than new in this batch, but it undermines the inspection
summary the batch added, so it is fixed here.

### Fixed: filename edge cases

Separators were trimmed before truncation, so a long name could yield a trailing
hyphen and a doubled separator before the date stamp. Slicing now happens first.

### Verification

- The v2.4 suite passes 91 checks. Automatic discovery passes 221, no failures,
  no skips.
- Eight deliberate regressions, one per fix, were each reverted on isolated copies
  of the artifact and the suite: all eight are caught. The shared worktree was
  never modified during those experiments.
- Catalog parity: 420 keys per language, identical key sets, zero placeholder
  mismatches, zero em dashes in the artifact.
- Privacy boundary intact, one non-empty `innerHTML` assignment, seven storage
  keys, every released v2.3 surface and tag unchanged.
- A preset named `<img src=x onerror=alert(1)>` renders as text in the dropdown
  and the inspection summary, with no element created.

### Confirmed correct, no change needed

Preset inspection does not load or alter the live setup. Duplicate preserves the
full validated configuration, respects the 50 ceiling, and leaks no session data.
Selective export contains exactly one preset with a clean envelope and leaves the
library untouched. Invalid stored presets cannot be loaded, renamed, duplicated,
or exported but can still be deleted, and the summary shows the localized invalid
message. Selection and the translated summary survive interface language changes
in all three languages. Filenames never derive from the question or any answer.

### Still open

A genuine NVDA or VoiceOver listening session. Everything else on the manual list
is now closed by the localhost browser method described above, which either of us
can repeat.

## ChatGPT closure of the deferred export cases

Claude deliberately left two edge cases for a product-policy decision. Both are
now resolved without changing the supported 50-preset ceiling.

- If browser storage was hand-edited to contain more than 50 valid presets,
  Export all now produces a directly importable file with the first 50. The
  visible status gives the number and names of valid extras left out and tells
  the user to use Export selected for them. Invalid entries are reported
  separately from valid overflow.
- The lower-level `exportPresetBundle` helper still refuses to truncate. Only
  the user-facing bulk workflow may produce the explicitly reported partial
  backup, so no programmatic caller can lose entries silently.
- Selected and bulk export failures clear any stale success line and use one
  dedicated localized explanation. The message no longer talks about importing
  an invalid file when the operation was an export.

The bulk integration check reads the actual downloaded Blob, confirms exactly
50 expected presets, confirms the named extra remains in storage, and proves the
download did not mutate the 51-entry library. The failure-path check covers both
export buttons and proves the stale status is cleared. The v2.4 suite passes 93
checks. Automatic discovery passes 223, with no failures or skips.

Two isolated negative controls were run outside the shared worktree. Restoring
the old above-50 abort makes the bulk-backup test fail immediately. Restoring
the import-oriented alert and stale status makes the failure-path test fail.
Both protections therefore have active regression coverage, and the temporary
copy was removed after the probes.

## Next checkpoint

The import correction checkpoint was committed as `209f9a4`, pushed to
`develop/v2.4.0`, and passed all 185 checks in GitHub Actions run `33163935079`.
Claude Code then implemented the recovery batch in `620bc2f`. ChatGPT's peer
hardening batch was committed as `380ae79`, pushed to `develop/v2.4.0`, and
passed all 200 checks in GitHub Actions run `33167076603`. Claude Code's
reciprocal correction batch was committed as `8844c13` and passed all 203 checks
in GitHub Actions run `33174485630`. ChatGPT's follow-up correction was committed
as `742d986` and passed all 204 checks in GitHub Actions run `33175597685`.

## Transcript filtering and lane navigation

The next long-relay batch adds attribute-first transcript filtering and direct
lane navigation without changing the session schema.

- Filters combine participant, turn type, round, state, and optional text.
  State filters cover stale, review-needed, excluded, trimmed, and ranked
  turns.
- The visible status reports how many saved turns are shown and how many are
  hidden. No-match results are distinguished from an empty transcript.
- French and Spanish label searches remove accents before matching. Focused
  checks prove that `revision` finds `révision` and `clasificacion` finds
  `clasificación`.
- Ranked filtering calls `effectiveBallot`. Replacing it with a raw array check
  makes the invalid-ranking test fail by exposing the rejected ballot.
- Lane stations are native buttons with localized labels and current-turn
  state. Reached turns are available, future turns are disabled, and invalid
  indexes are inert.
- Lane navigation changes only the cursor and ended state. A falsification run
  that called `saveCurrent` during station activation failed because it
  committed an unsaved draft, proving the no-answer-mutation test is active.
- Removing accent folding also fails the focused French search assertion.
- Reduced-motion users receive instant lane centering instead of scripted
  smooth scrolling.

The draft now has 396 matching keys in each language catalog. The focused v2.4
suite has 80 passing checks, and full automatic discovery passes 210 checks
with no failures or skips.

The browser pass covered a four-turn Draft and improve relay in English,
French, and Spanish. Participant, combined kind, and text filters returned the
expected entries and counts. Mouse, Enter, and Space station activation moved
to the intended saved turns without changing the four captured answers. The
first desktop render exposed crowded four-column filters in the transcript
card, so the layout was corrected to two columns on desktop and one at phone
width. The repeated desktop and 390 by 844 checks have no horizontal overflow,
clipped control labels, console warnings, or console errors.

## Preset library and export polish

The next product batch completes the practical preset-management and download
items from the roadmap.

- Selecting a preset now shows a text-only inspection summary before it is
  loaded. The summary covers its plan, rounds, response format, prompt
  language, participant names, and roles. Invalid stored data is identified as
  untrusted and cannot be loaded, renamed, duplicated, or selectively
  exported, while Delete remains available so the bad entry can be removed.
- Rename rejects case-insensitive collisions and writes storage once only after
  all validation passes. Duplicate preserves the complete validated
  configuration, uses deterministic collision suffixes, and obeys the
  50-preset ceiling. Neither operation can retain a question, answer, generated
  prompt, or unknown field.
- A selected preset can be exported by itself. Exporting never changes the
  saved library. Less common library actions are grouped in one disclosure to
  reduce setup-screen clutter.
- Session, transcript, review packet, selected-preset, and all-preset downloads
  now use portable date-stamped filenames. The filename helper strips unsafe
  characters and never derives names from session questions or answers.
- Relay downloads announce the requested filename in a visible polite status.
  Preset actions reuse their existing status channel and now also confirm Save,
  Load, Rename, Duplicate, Delete, and export results.
- An inherited failure path was corrected: deleting a preset previously
  ignored a failed local-storage write. It now reports the failure and leaves
  the stored library intact.

Focused coverage proves helper immutability, one-write success behavior,
collision rejection, duplicate limits, deletion failure handling, localized
inspection, invalid-storage isolation, filename portability, and visible
download confirmation. The v2.4 suite passes 86 checks. Full automatic
discovery passes 216 checks with no failures or skips.

Two focused negative controls prove that the new integration checks are live.
Ignoring a failed preset-deletion write makes the management test fail on the
missing warning. Replacing selected-only export with the complete library makes
the export test fail by exposing the second preset. Both guards were restored,
and the two focused checks returned to 2 of 2 passing.

The in-app browser controller refused the local `file:` preview under its URL
security policy during this round. No alternate browser-control route was used.
The new preset disclosure and inspection summary still require a human visual
pass at desktop, narrow phone width, and high zoom. The earlier transcript and
lane browser evidence remains valid because those surfaces were not changed.

# Relay Console v2.3.0 progress

## Baseline verified on 28 August 2026

- `main` and `origin/main` both point to `a9a6199`.
- The working tree was clean before the isolated worktree was created.
- The v2.0, v2.1, and v2.2 suites passed with 86 checks total.
- No GitHub issue is currently open.
- The v2.2 release artifact and repository checksum remain unchanged.

## Planning audit completed

- Claude Code completed a read-only independent scope and risk audit.
- The audit confirmed portable presets as the strongest small release theme.
- Source review confirmed the unvalidated preset URL and role path, incomplete
  preset preference persistence, inconsistent rounds fallback, stale quick-start
  status, incomplete no-em-dash test coverage, and v2.3 CI migration traps.
- A redundant fourth quick start was rejected.
- A full prompt-inclusive review packet was rejected because of injection,
  privacy, localization, and quadratic-size risks.

## Implementation boundary

Development is isolated in the `develop/v2.3.0` worktree. Earlier release
artifacts remain immutable. Implementation, automated verification, Claude Code
review, browser review, and publication evidence will be added here as the cycle
continues.

## First implementation pass completed on 28 August 2026

- Added a versioned preset bundle with strict validation, a 1 MB file limit,
  a 50-preset limit, atomic storage, and collision-safe names.
- Routed both imported and existing local presets through the same normalizer.
- Restricted imported participant URLs to HTTP and HTTPS and bounded names,
  roles, rosters, custom steps, and rounds.
- Repaired complete setup persistence, the rounds fallback, and stale
  quick-start status.
- Added a prompt-language review packet with a visible privacy warning,
  packet-specific marker protection, actual context decisions, and no generated
  prompt duplication.
- Added a v2.3 suite with 44 checks and wired it into every CI path. The four
  suites pass independently with 127 checks total.
- Completed browser checks at desktop and phone widths with no horizontal
  overflow. Verified quick starts, editable status clearing, Spanish interface,
  French prompts, a complete three-turn relay, preset import, collision naming,
  preset loading, and preset export. No browser console warnings or errors were
  recorded.
- Corrected a stale v2.2 version badge found during browser inspection.

## First audit checkpoint completed

The development branch was committed and published, then Claude Code received
an independent read-only audit request against the roadmap and test claims.

## Independent audit received on 28 August 2026

Claude Code confirmed the branch, remote commit, clean tree, CI run, all 127
checks, strict validation boundaries, privacy properties, catalog parity, and
unchanged release artifacts. Three reproducible repair items were accepted:

- Preserve quick-start guidance when only the interface language changes.
- Avoid repeating an untrimmed answer inside the review packet.
- Export every valid stored preset even when another stored entry is malformed,
  while reporting the skipped entry.

The repair pass also aligns case-insensitive preset naming, rejects session files
from the preset-only button, adds real setup-event and oversized-file tests,
makes the no-em-dash scan recursive, clears old status notices automatically,
and records the checksum assertion migration as a release gate.

## Repair verification completed

- The v2.3 suite now contains 47 checks. All four suites pass independently
  with 130 checks total and no failures.
- Desktop browser checks confirmed that interface-language changes preserve
  quick-start guidance and actual setup edits clear it.
- The preset-only import button rejects session files without changing the
  current screen, and valid import status clears automatically.
- Phone-width verification at 390 by 844 pixels found no horizontal overflow.
- French import wording is correct, and the browser console remained free of
  warnings and errors.

## Final independent re-audit completed

Claude Code independently exercised all ten requested verification points at
commit `68634e3`. The audit found no release-blocking issue, no other
reproducible defect, and recommended proceeding with the v2.3.0 release
candidate. Release advisories were accepted for the final rename checklist,
SVG text coverage, and phone-width stress testing of the longest preset status.

## Release candidate completed on 28 August 2026

- Renamed the audited draft to `relay-console-v2.3.0.html` and removed every
  active draft label from the final product and release assertions.
- Made `index.html` byte-identical to the v2.3.0 artifact.
- Moved the live index and checksum-manifest integrity assertions from the
  v2.2 suite into v2.3, with eight ordered release entries.
- Extended the no-em-dash scan to the workflow SVG and remaining root text
  files, and expanded CI triggers to all documentation and test files.
- Updated the README, landing page, changelog, release notes, roadmap, progress
  record, and checksum manifest for v2.3.0.
- Re-ran all four suites with 130 passes and no failures.
- At 390 by 844 pixels, the longest skipped-preset notice wrapped inside its
  card with no horizontal overflow, cleared after six seconds, and produced no
  browser console warning or error.
- Final SHA-256:
  `d02e5753b27f96dd9c9183e10f9f975bed2f24f3de96e10ee759221f1fa21ef5`.

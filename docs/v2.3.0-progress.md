# Relay Console v2.3.0 progress

## Baseline verified on 28 August 2026

- `main` and `origin/main` both point to `a9a6199`.
- The working tree was clean before the isolated worktree was created.
- The v2.0, v2.1, and v2.2 suites passed with 86 checks total.
- No GitHub issue is currently open.
- The v2.2 release artifact and repository checksum remain unchanged.

## Planning audit completed

- Claude Code completed a read-only independent scope and risk audit.
- The audit confirmed portable presets as the strongest small release theme.
- Source review confirmed the unvalidated preset URL and role path, incomplete
  preset preference persistence, inconsistent rounds fallback, stale quick-start
  status, incomplete no-em-dash test coverage, and v2.3 CI migration traps.
- A redundant fourth quick start was rejected.
- A full prompt-inclusive review packet was rejected because of injection,
  privacy, localization, and quadratic-size risks.

## Implementation boundary

Development is isolated in the `develop/v2.3.0` worktree. Earlier release
artifacts remain immutable. Implementation, automated verification, Claude Code
review, browser review, and publication evidence will be added here as the cycle
continues.

## First implementation pass completed on 28 August 2026

- Added a versioned preset bundle with strict validation, a 1 MB file limit,
  a 50-preset limit, atomic storage, and collision-safe names.
- Routed both imported and existing local presets through the same normalizer.
- Restricted imported participant URLs to HTTP and HTTPS and bounded names,
  roles, rosters, custom steps, and rounds.
- Repaired complete setup persistence, the rounds fallback, and stale
  quick-start status.
- Added a prompt-language review packet with a visible privacy warning,
  packet-specific marker protection, actual context decisions, and no generated
  prompt duplication.
- Added a v2.3 suite with 44 checks and wired it into every CI path. The four
  suites pass independently with 127 checks total.
- Completed browser checks at desktop and phone widths with no horizontal
  overflow. Verified quick starts, editable status clearing, Spanish interface,
  French prompts, a complete three-turn relay, preset import, collision naming,
  preset loading, and preset export. No browser console warnings or errors were
  recorded.
- Corrected a stale v2.2 version badge found during browser inspection.

## Next checkpoint

Commit and publish the development branch, then ask Claude Code for an
independent read-only implementation audit against the roadmap and test claims.

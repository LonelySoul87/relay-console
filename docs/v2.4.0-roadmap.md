# Relay Console v2.4.0 roadmap

Status: core safety and long-relay navigation implemented on
`develop/v2.4.0`, with reciprocal review in progress.

## Product goal

Polish the complete daily workflow without weakening Relay Console's defining
boundary: one local, offline, inspectable HTML file with the user in control of
every handoff.

The release should prefer improvements that reduce mistakes, make longer relays
easier to navigate, and help a new user understand what will happen before any
session or preset data is replaced.

## Joint scope decision

The v2.4 theme is **Nothing is lost by accident**. ChatGPT and Claude agreed to
prioritize import review, bounded recovery, honest autosave status, ballot
correctness, and deliberate destructive actions before adding broader library
or language features.

The four audited defect fixes, import preview, bounded recovery, save status,
storage visibility, transcript filters, and lane navigation are implemented.
The recovery work completed reciprocal hardening before the transcript and lane
batch began.

A fourth language is deferred to v2.5 so it can receive a complete translation,
prompt, ballot, accessibility, and layout review. Right-to-left support remains
a separate architectural release decision. Generated diagnostics and telemetry
are rejected for v2.4.

## Candidate priority tracks

These tracks are proposals for joint ChatGPT and Claude review. They are not all
promised until their interaction, storage, localization, and test costs are
measured.

### 1. Safer file review before import

- Implemented in the draft: selected sessions and preset bundles are parsed,
  validated, and summarized before the user can apply them.
- The preview shows version, participant count, progress, languages, preset
  count, naming collisions, dropped unsafe URLs, and shortened roles.
- Previewing changes neither the active relay nor local storage. Applying a
  preset bundle performs one storage write.
- Imported content reaches the preview only through text nodes, never HTML.

### 2. Better preset library management

- Consider rename, duplicate, reorder, inspect, selective export, and clearer
  bulk management.
- Preserve case-insensitive collision safety and the 50-preset ceiling.
- Keep questions, answers, prompts, and session markers outside every preset.

### 3. Faster transcript navigation

Delivered. The decisions taken while implementing it:

- Filters combine participant, turn type, round, state, and optional text
  matching. State choices cover stale, review-needed, excluded, trimmed, and
  ranked turns.
- The ranked filter uses the same validated ballot helper as the tally and
  exports. A non-empty but invalid stored array never counts as ranked.
- Text matching searches captured text and rendered labels with accent and case
  folding, so unaccented French and Spanish queries still find localized terms.
- Filtering exists only in the rendered view. It never changes captured
  answers, forwarded context, exports, or saved session data.
- A polite status reports the visible, total, and hidden counts. A distinct
  empty result explains that saved turns exist but none match.
- Lane stations are native buttons. Reached turns can be opened directly by
  mouse or keyboard, future turns stay disabled, and moving through the lane
  never commits an unsaved draft as a captured answer.
- The lane remembers the furthest turn reached during the current page session,
  so jumping backward does not strand the user there.
- Smooth centering respects reduced-motion preferences.

### 4. Recovery and autosave confidence

Delivered. The decisions taken while implementing it:

- One slot at `relayConsole.recovery.v1`, never a list. A second capture
  replaces the first, so the feature cannot become a growing archive of
  abandoned relays.
- Seven-day expiry, checked on read rather than on a timer. An expired or
  unusable record is deleted the moment it is seen, so a stale copy cannot
  outlive its window just because the app was closed.
- One megabyte serialized ceiling. Above it the checkpoint is refused whole and
  the user is told to use Save file. A truncated relay would be worse than none,
  because it would look restorable and would not be.
- A checkpoint is only written when the work about to be lost is meaningful, by
  the same test the import replacement guard already uses. Empty relays leave no
  trace.
- Restoring validates through `validateSession`, the same path as any imported
  file. Recovery data is treated as untrusted, because it can be edited by hand
  in browser storage.
- The autosave timestamp only advances on a save that actually succeeded. A
  failure stays visible until a later save works.
- Storage reporting is recomputed on preset and recovery changes, not on every
  keystroke, so a long relay does not pay to serialize every slot while typing.
- A failed or oversize recovery write pauses the destructive action and asks
  whether to continue. Cancel leaves the current work intact.
- A restored checkpoint is consumed only after the restored relay autosaves
  successfully.
- Starting a new relay also protects any saved relay currently offered by the
  resume bar.
- Storage values are explicitly estimates based on the browser strings Relay
  Console owns. Browser quotas vary and are not presented as a fixed capacity.

### 5. Accessibility and small-screen polish

- Audit focus movement when starting, advancing, going back, importing, and
  completing a relay.
- Consider a keyboard-shortcut reference that is discoverable without clutter.
- Verify high zoom, narrow screens, long translations, contrast, reduced motion,
  status announcements, and touch target sizes.
- Completed for the recovery checkpoint: a 390 by 844 pixel browser pass covers
  simultaneous resume and recovery banners, an unbroken 60-character question,
  the longest Spanish storage line, safe removal cancellation, and browser
  warnings and errors.

### 6. Export and handoff polish

- Improve filenames, visible export status, selective preset export, and
  clearer confirmation after an export.
- Keep transcript and review exports linear in session size.
- Preserve the strict separation between captured answers, forwarded context,
  generated prompts, and configuration-only preset data.

### 7. Language expansion only with complete quality

- Evaluate one additional complete interface and prompt language only if key
  parity, native wording, layout review, and ballot behavior can be verified.
- Do not ship a partial translation or weaken English, French, or Spanish.
- Treat right-to-left support as a separate layout decision, not a string-only
  change.

## Explicitly out of scope

- No model APIs, backend, account, remote storage, analytics, or trackers.
- No automated reading from or writing into chatbot tabs.
- No sharing URLs containing questions, answers, prompts, or session data.
- No dependency, build step, service worker, or multi-file runtime requirement.
- No generated prompts inside preset bundles.
- No large feature whose privacy or recovery behavior cannot be tested.
- No new storage key without a stated size bound and a visible removal control.

## Quality boundary

- Preserve v1.x and v2.x session compatibility.
- Preserve the complete v2.3 feature set and three-language catalog parity.
- Give v2.4 a dedicated regression suite from the first scaffold commit.
- Make CI discover every matching versioned suite automatically.
- Keep active text free of the Unicode em dash character.
- Run every suite independently and in the same discovery mode used by CI.
- Complete desktop, phone-width, keyboard, and high-zoom browser checks.
- Use Claude Code for independent scope, implementation, and release-candidate
  audits.

## Release migration gate

At release-candidate time, and only after the draft passes its audit:

- Rename the draft artifact and update every draft-bound v2.4 assertion.
- Move the live `index.html` byte-identity and checksum-manifest assertions from
  v2.3 into v2.4 in the same commit.
- Extend the ordered manifest to nine release artifacts.
- Update `index.html`, `SHA256SUMS.txt`, release notes, audit, public copy, and
  the immutable artifact together.
- Re-hash the final bytes, verify the public asset after upload, and keep all
  earlier release artifacts and tags unchanged.

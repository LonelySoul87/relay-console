# Relay Console v2.0.0-draft Audit

Date: 2026-07-08

Artifact under review: `relay-console-v2.0.0-draft.html`

## Verified

- Draft artifact is present at the repo root.
- SHA-256 is listed in `SHA256SUMS.txt`:
  `bb4712fce29d7e032fa2fad6d803aa01d933141e134d3793ae49eecac92af5df`.
- Embedded JavaScript in `relay-console-v1.9.0.html` and
  `relay-console-v2.0.0-draft.html` parses successfully with the bundled Node
  runtime.
- v2.0.0-draft keeps the same Content-Security-Policy string as v1.9.0.
- Static network scan found no `fetch`, `XMLHttpRequest`, `sendBeacon`,
  `WebSocket`, `EventSource`, dynamic import, worker, external script, external
  stylesheet, image, or iframe usage. The only `https://` strings are editable
  provider homepage suggestions.
- `validateSession()` contains fallback handling for v1-style `mode` sessions
  and v2 recipe/ballot fields.
- `.gitattributes` now marks versioned HTML release artifacts as `-text` so
  future release checksums are not changed by line-ending normalization.
- Chrome extension smoke test passed against the same artifact served from
  `http://127.0.0.1:8765/relay-console-v2.0.0-draft.html`:
  - opened the v2 draft in Chrome;
  - started a Ballot panel relay;
  - captured four blind answers;
  - parsed four explicit `RANKING: A > B > C > D` ballot replies;
  - showed the scoreboard (`Claude 12`, `ChatGPT 8`, `Gemini 4`,
    `Perplexity 0`);
  - included the Borda tally in the synthesis prompt;
  - completed the relay with stats and transcript visible;
  - reported no Chrome console errors.
- Manual Chrome `file://` smoke passed:
  - opened `relay-console-v2.0.0-draft.html` directly from the local project
    folder;
  - confirmed the URL used the `file:///E:/France/Stage/Github Projects/Relay Console/...`
    launch path;
  - started a relay from the local file;
  - confirmed the run view, relay lane, turn card, and generated prompt appeared.

## Not Rerun

- Claude's reported 49/49 headless checks were not rerun here.
- A local Playwright smoke test was attempted, but the bundled `playwright`
  package is missing `playwright-core` in this environment.
- Direct automated navigation to the `file://` artifact was blocked by the
  browser-use URL policy in both the in-app browser and Chrome surfaces. The
  automated Chrome smoke therefore used a temporary localhost static server and
  then shut it down; the `file://` launch path was verified manually.

## Notes

- Existing `SHA256SUMS.txt` entries for v1.8.4 and v1.9.0 are treated as
  published release checksums. The current Windows working tree has
  `core.autocrlf=true`, so those checked-out file bytes do not match the
  published hashes even though Git reports the files clean. The new
  `.gitattributes` rule is intended to prevent that for future artifacts after
  the rule is committed and the files are rechecked out or renormalized.
